server:
  port: 8080

# DataSource Config
spring:
  application:
    name: jomkie-sbmp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?useSSL=false&useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull
    username: root
    password: root

mybatis-plus:
  configuration:
    auto-mapping-behavior: full
